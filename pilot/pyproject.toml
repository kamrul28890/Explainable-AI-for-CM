[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "xai-pilot"
version = "0.1.0"
description = "Florence-2 XAI pilot for ConstructionSite 10k (Abdallah six-metric framework adaptation)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
