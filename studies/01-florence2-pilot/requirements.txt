# torch must be installed FIRST and separately, pinned to the exact CUDA build, e.g.:
#   pip install torch==2.12.1+cu130 --index-url https://download.pytorch.org/whl/cu130
# (check https://download.pytorch.org/whl/ for the latest cuXXX tag matching this torch version --
#  installing the rest of this file can silently pull a CPU-only torch wheel and overwrite it)
torch==2.12.1
# transformers>=5.0 removed legacy generation attrs (e.g. forced_bos_token_id)
# from PretrainedConfig, which crashes Florence-2's pinned remote code
# (configuration_florence2.py) on load. 4.49.0 is the last 4.x release and is
# the version this pilot actually runs on -- verified via scripts/01_check_environment.py.
transformers==4.49.0
huggingface_hub==0.36.2
tokenizers==0.21.4
datasets==4.5.0
accelerate
captum
opencv-python
Pillow
numpy
pandas
matplotlib
seaborn
scikit-image
scipy
pytest
jupyter
# ipykernel 7.x rewrote the shell channel as async and added the subshell protocol.
# VS Code's Jupyter extension (ms-toolsai.jupyter 2025.9.1) predates that and hangs
# forever on "Connecting to kernel..." against a 7.x kernel -- while nbconvert and
# jupyter_client work fine, which makes it look like a notebook bug rather than a
# client mismatch. Pinned until the extension is known to support 7.x.
ipykernel<7
einops
timm
